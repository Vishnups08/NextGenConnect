import 'package:flutter/material.dart';

class MentorReportsPage extends StatelessWidget {
  const MentorReportsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mentor Reports'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('Mentor Reports Page Placeholder'),
      ),
    );
  }
} 